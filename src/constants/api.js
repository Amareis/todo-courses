const HTTPS = 'https://'

const GITHUB_ROOT = 'api.github.com/'

const GITHUB_USERS = 'users'
const GITHUB_ORGANIZATIONS = 'organizations'

export const API_USERS = HTTPS + GITHUB_ROOT + GITHUB_USERS
export const API_ORGANIZATIONS = HTTPS + GITHUB_ROOT + GITHUB_ORGANIZATIONS